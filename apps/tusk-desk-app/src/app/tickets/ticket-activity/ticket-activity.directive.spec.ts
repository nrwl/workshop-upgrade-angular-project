import { TicketActivityDirective } from './ticket-activity.directive';

describe('TicketActivityDirective', () => {
  it('should create an instance', () => {
    const directive = new TicketActivityDirective();
    expect(directive).toBeTruthy();
  });
});
