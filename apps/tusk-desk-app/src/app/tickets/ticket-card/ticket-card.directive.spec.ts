import { TicketCardDirective } from './ticket-card.directive';

describe('TicketCardDirective', () => {
  it('should create an instance', () => {
    const directive = new TicketCardDirective();
    expect(directive).toBeTruthy();
  });
});
